interface:
  display_name: "TalentModel Skill"
  short_description: "Repo-specific patterns and workflows for TalentModel-skill"
  default_prompt: "Use the TalentModel-skill repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true